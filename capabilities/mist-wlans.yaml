naftiko: 1.0.0-alpha2
info:
  label: Juniper Networks Juniper Mist Cloud API — WLANs
  description: 'Juniper Networks Juniper Mist Cloud API — WLANs. 2 operations. Lead operation: Juniper Networks List site
    WLANs. Self-contained Naftiko capability covering one Juniper Networks business surface.'
  tags:
  - Juniper Networks
  - WLANs
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    JUNIPER_NETWORKS_API_KEY: JUNIPER_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: mist-wlans
    baseUri: https://api.mist.com/api/v1
    description: Juniper Networks Juniper Mist Cloud API — WLANs business capability. Self-contained, no shared references.
    resources:
    - name: sites-site_id-wlans
      path: /sites/{site_id}/wlans
      operations:
      - name: listsitewlans
        method: GET
        description: Juniper Networks List site WLANs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createsitewlan
        method: POST
        description: Juniper Networks Create WLAN
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.JUNIPER_NETWORKS_API_KEY}}'
  exposes:
  - type: rest
    namespace: mist-wlans-rest
    port: 8080
    description: REST adapter for Juniper Networks Juniper Mist Cloud API — WLANs. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/sites/{site-id}/wlans
      name: sites-site-id-wlans
      description: REST surface for sites-site_id-wlans.
      operations:
      - method: GET
        name: listsitewlans
        description: Juniper Networks List site WLANs
        call: mist-wlans.listsitewlans
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createsitewlan
        description: Juniper Networks Create WLAN
        call: mist-wlans.createsitewlan
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mist-wlans-mcp
    port: 9090
    transport: http
    description: MCP adapter for Juniper Networks Juniper Mist Cloud API — WLANs. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: juniper-networks-list-site-wlans
      description: Juniper Networks List site WLANs
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: mist-wlans.listsitewlans
      outputParameters:
      - type: object
        mapping: $.
    - name: juniper-networks-create-wlan
      description: Juniper Networks Create WLAN
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mist-wlans.createsitewlan
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
